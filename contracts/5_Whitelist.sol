// SPDX-License-Identifier: MIT
pragma solidity 0.8.26;

contract Whitelist {
    mapping (address => bool) whitelist;

    // ex array dans mapping  
    // mapping (address => uint[]) students;
}